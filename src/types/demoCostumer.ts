export interface DemoCostumer {
      firstName: string;
      lastName: string;
      email: string;
      phoneNumber: string;
      jobTitle: string;
      country: string;
      message: string;
};